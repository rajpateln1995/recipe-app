Recipe web app that I made while learning angular 8

Follow the steps to run the app.

1. open command.
2. change directory to project folder.
3. If angular cli and node.js already installed.
-> Run commmand npm install.
-> Run commmand Hit ng serve
-> Type url http://localhost:4200 in browser.

